<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PasswordReader.Pages.NoteListPage"
             Title="Notizen">
    <ScrollView>
        <Grid RowDefinitions="*,Auto,Auto" Margin="20" RowSpacing="10">
            <FlexLayout BindableLayout.ItemsSource="{Binding NoteItems}" Wrap="Wrap" AlignContent="Start">
                <BindableLayout.ItemTemplate>
                    <DataTemplate>
                        <Button Text="{Binding Title}" HeightRequest="40" Clicked="NoteItem_Clicked" Margin="2"/>
                    </DataTemplate>
                </BindableLayout.ItemTemplate>
            </FlexLayout>
            <Button Grid.Row="1" Text="Neue Notiz anlegen" Clicked="NewNoteItem_Clicked" WidthRequest="200" IsEnabled="{Binding CanCreateNote}"/>
            <ActivityIndicator Grid.Row="2" IsRunning="{Binding IsRunning}"/>
        </Grid>
    </ScrollView>
</ContentPage>