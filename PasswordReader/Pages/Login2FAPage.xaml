<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PasswordReader.Pages.Login2FAPage"
             Title="Sicherheitscode">
    <ScrollView>
        <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="120,*" Margin="20" RowSpacing="20">
            <Label Text="Sicherheitscode:" VerticalOptions="Center"/>
            <Entry Text="{Binding SecurityCode}" Grid.Column="1"
               IsEnabled="{Binding CanConfirmSecurityCode}"
               />
            <Button Text="Bestätigen" Clicked="Confirm_Clicked" Grid.Row="2" Grid.ColumnSpan="2" WidthRequest="100" 
                IsEnabled="{Binding CanConfirmSecurityCode}"/>
        </Grid>
    </ScrollView>
</ContentPage>